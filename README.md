# Tiffin Management System

## Structure

- `backend/`: FastAPI application, domain models, schemas, routes, and services.
- `frontend/`: React application.

## Run the backend

```bash
uvicorn backend.app.main:app --reload
```
